version: '3'

services:
  container:
    image: thnderbread/vodskipper:vodskipper-test
    options: --shm-size=2gb
    privileged: true
    tty: true
    volumes:
      - ./tests:/app/.tests

    # all that's left is to try splitting mocks and tests
    # and to try publishing the extension
    